# クイック環境設定

[![Direct](https://img.shields.io/badge/Direct%20Link-AiQuickPrefsPalette.jsx-ffcc00.svg)](https://github.com/swwwitch/illustrator-scripts/blob/master/jsx/preference/AiQuickPrefsPalette.jsx)

[![Direct](https://img.shields.io/badge/Back%20to%20home-All%20scripts-cccccc.svg)](https://github.com/swwwitch/illustrator-scripts/blob/master/README.md)

---

「線幅と効果も拡大・縮小」を切り替えたい。「プレビュー境界」を一時的にオンにしたい。矢印キーの移動量を 1mm にしたい。

どれも作業中に何度も発生するのに、そのたびに環境設定ダイアログを開いて、目的のカテゴリを探して、チェックを入れて、［OK］。しかも項目は「一般」「単位」「選択範囲・アンカー表示」とバラバラの場所に散っていて、**普段よく触る数個だけが遠い**という状態です。

そこで、よく使う項目だけを1枚のパレットに集めて、クリックした瞬間に反映されるようにしました。開きっぱなしにしておく前提のパレットです。

<img alt="" src="" width="50%" />

## 使い方

スクリプトを実行するとパレットが開きます。あとはチェックボックスやボタンを操作するだけで、**その時点で即座に反映**されます。

**［OK］も［適用］もありません。** 押した瞬間が確定です。パレットは `Esc` キー（パレットがアクティブなとき）でも閉じられます。

すでにパレットが開いている状態でもう一度実行すると、新しく開き直さずに既存のパレットを前面に出します。

## 操作できる項目

| パネル | 項目 | 内容 |
| --- | --- | --- |
| キー増加 | 数値＋単位 | 矢印キーでの移動量（環境設定 > 一般 > キー増加）。単位ポップアップで定規単位を切り替え |
| 整列オプション | プレビュー境界 | 整列・分布で線幅・効果を含む境界を使う |
| 字形の境界に整列 | ポイント文字 / エリア内文字 | 文字を字形の境界で整列 |
| 変形オプション | パターン / 角 / 線幅と効果 | 変形時にパターンも変形、拡大・縮小時に角（ライブコーナー）の半径・線幅と効果も追従 |
| 変形 | 水平反転 / 垂直反転 / 45°回転 | 選択オブジェクトへの操作（後述） |
| アートボード | アートボード名を表示 / ビデオ定規の表示 | ビデオ定規はボタン（トグル） |
| アートボードの枠線 | ハイライトのカラー / ストロークの幅 | カラーは9色から、幅は 1〜4 |
| コピー/ペースト | 書式なしペースト / コピー元のレイヤーにペースト | |
| 描画 | リアルタイムの描画と編集 / プレビュー更新 | プレビュー更新は GPU プレビューの再描画ボタン |

各項目にはツールチップが付いているので、どの環境設定に対応するかはポインタを重ねれば確認できます。

## Option+クリックで一括切替

「字形の境界に整列」と「変形オプション」のチェックボックスは、**Option+クリックでそのパネル内の全項目が同じ状態になります**。

- 「字形の境界に整列」：ポイント文字／エリア内文字の2つ
- 「変形オプション」：パターン／角／線幅と効果の3つ

通常のクリックはこれまでどおり個別切替です。「変形オプション」は3つまとめてオン／オフしたい場面が多いので、Option を添えるだけで済みます。

## キー増加の数値入力

数値欄では `↑` `↓` キーで増減できます。

| 操作 | 増減 |
| --- | --- |
| `↑` `↓` | ±1 |
| `Shift` + `↑` `↓` | ±10（次の10の倍数へスナップ） |
| `Option` + `↑` `↓` | ±0.1 |

負の値にはならず、0 でクランプされます。

単位ポップアップは表示単位を変えるだけのものではなく、**定規単位（rulerType）そのものを切り替えます**。ここが少し分かりにくいところです。単位を切り替えても、保存されている移動量の実寸（pt）は変わりません。表示だけが新しい単位で計算し直されます。

ポップアップに並ぶのは in / mm / pt / pica / cm / Q/H / px の7つです。

## 変形パネル

「水平反転」「垂直反転」「45°回転」は、いずれも**選択オブジェクト全体の可視バウンディングの中心を基点**にします。個々のオブジェクトがそれぞれの中心で回るのではなく、選択全体がひとかたまりとして反転・回転します。

45°回転の方向は右側のラジオボタン（時計回り／反時計回り）で指定します。デフォルトは反時計回りです。

なお、バウンディングが取得できない／変形できない項目（ロック、非表示、ガイドなど）はスキップされます。

## 外部での変更に追従する

環境設定ダイアログなど、パレットの外で設定を変更した場合は、**パレットをクリック（再アクティブ）すれば表示が同期します**。

ただし、キー増加の数値欄にフォーカスがある間は同期しません。入力中の値が上書きされてしまうのを防ぐためです。

## アートボードの枠線カラー

ドロップダウンには9色のプリセットが並びます（ライトブルー／サーモンピンク／グリーン／ミディアムブルー／マゼンタ／シアン／ライトグレー／ブラック／イエロー）。

現在の設定値がプリセットに一致しない場合は、**RGB がもっとも近いプリセットが選択された状態で表示されます**。任意の色を指定するためのものではなく、よく使う色を素早く選ぶためのドロップダウンです。

## 補足

Illustrator の常駐パレットは、表示している間に DOM への接続を失うという事情があるため、環境設定の**書き込み**とオブジェクトの変形はすべて BridgeTalk 経由でメインエンジンに委譲しています。一方、**読み出し**はエンジンを跨いでも安全なため、パレット側で直接・同期的に取得しています。

反転・回転は「平行移動 → 反転／回転 → 平行移動」の3ステップを1つの合成行列にまとめ、`transform` の呼び出しをオブジェクトあたり1回に減らしています。cos / sin はパレット側で数値として計算してからコードに埋め込んでいます。

「角」だけは Boolean ではなく整数のプリファレンス（`policyForPreservingCorners`、1=ON / 2=OFF）なので、一括切替の共通処理には「値を書き込む関数」を項目ごとに渡す形にして吸収しています。

「プレビュー更新」ボタンは、メニューコマンド `View using GPU` を2回トグルして再描画を起こしています。アートボードの枠線を変更したときも、`zoomout` → `zoomin` でキャンバスを描き直しています。

## 更新履歴

- v1.8.1（20260629）「字形の境界に整列」（2つ）と「変形オプション」（3つ）の各チェックボックスを Option+クリックでグループ一括切替に対応。連動処理を linkCheckboxGroup に共通化（角は整数プリファレンスを apply 関数で吸収）。
- v1.8.0（20260628）「45°回転」ボタンを追加（方向はラジオで指定・デフォルト反時計回り・選択中心基準）。反転ボタンを水平／垂直で横並び化し「水平反転／垂直反転」へ改称、パネル名を「変形」に短縮。整列パネルに「整列オプション」タイトルを付与、アートボードパネル名を「アートボード」に短縮。2カラム間隔を COLUMN_SPACING で調整。
- v1.7.1（20260627）「その他」を「コピー/ペースト」へ改称し、「描画」パネルを新設（リアルタイムの描画と編集を移動）。描画パネルにプレビュー更新ボタンを追加。
- v1.7.0（20260627）変形（反転）パネルを追加（選択中心基準・ロック／ガイド等はスキップ・合成行列で高速化）。レイアウト刷新（左＝キー入力／整列／字形の境界、右＝変形オプション／変形（反転））。テキスト／単位パネルとカンバスカラーを削除、「コピー元のレイヤーにペースト」を追加。esc キーで閉じる、編集中は同期を回避。命名整理、チェックボックス生成・ボタン高さ調整の共通化。
- v1.6.0（20260627）標準フォーマットへ整理（IIFE 化、ローカライズ構造、ブロックコメント）。パレット化＋BridgeTalk 委譲、キー入力の単位ポップアップ、クリック同期、ガイド／アートボードパネルを追加、2カラムレイアウト。
- v1.0（20250804）初期バージョン。

### note

- [記事URL]
