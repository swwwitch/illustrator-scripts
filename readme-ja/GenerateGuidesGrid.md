# グリッド状にガイドを生成

[![Direct](https://img.shields.io/badge/Direct%20Link-GenerateGuidesGrid.jsx-ffcc00.svg)](https://github.com/swwwitch/illustrator-scripts/blob/master/jsx/guide/GenerateGuidesGrid.jsx)

[![English](https://img.shields.io/badge/README-English-4b8bbe.svg)](https://github.com/swwwitch/illustrator-scripts/blob/master/readme-en/GenerateGuidesGrid.md)

[![Direct](https://img.shields.io/badge/Back%20to%20home-All%20scripts-cccccc.svg)](https://github.com/swwwitch/illustrator-scripts/blob/master/README.md)

---

### 概要

- アートボードまたは選択オブジェクトの外接矩形を、指定した行数・列数に分割してグリッド用のガイドを作成するスクリプト。
- セルの長方形化、角丸、中心点の表示、プリセットの読み込み／書き出しにも対応。

### ムービー

[ムービー](https://youtu.be/7sNMYhyTwfY)

<img alt="" src="https://www.dtp-transit.jp/images/ss-738-1182-72-20250713-082813.png" width="80%" />

### 主な機能

- 対象を「選択オブジェクト」「アクティブなアートボード」「すべてのアートボード」から選択
- 行数・列数と、行間・列間（ガター）の指定。ガターは連動も可能
- 上下左右のマージンを個別に指定。連動チェックで一括適用
- ガイドをアートボードの外へ伸ばす「伸張」の指定
- アートボードの上下左右4辺にガイドを引く「アートボードのエッジ」
- 各セルの左右中央に縦ガイドを引く「セルを分割」
- セルの長方形化（別レイヤー・不透明度・角丸・中心点の表示）
- 既存ガイドの削除（チェックした時点でプレビューに反映）
- プリセットの読み込みと、現在の設定の書き出し
- 設定変更に追従するライブプレビュー
- アウトライン表示／プレビュー表示の切り替え
- 日本語／英語UIの自動切り替え

### 使い方

1. 対象にするオブジェクトを選択（またはアートボードをアクティブに）してスクリプトを実行
2. ダイアログで行数・列数、マージンなどを設定（変更はライブプレビューに反映）
3. ［OK］で確定

### オプション

- **対象**：選択オブジェクト／アクティブなアートボード／すべてのアートボード
- **元のオブジェクト**（対象が選択オブジェクトのとき）：削除する／残す／ガイド化する
- **長方形化**：セルを長方形として描画。不透明度はスライダー、角丸は数値で指定
- **中心を表示**：作成した長方形に中心点を表示
- **ガイドを引く**：グリッドのガイドを描画
- **ガイドの伸張**：アートボードの外へ伸ばす距離
- **アートボードのエッジ**：アートボードの4辺にガイドを描画（対象が選択オブジェクトのときはディム）
- **セルを分割**：各セルの左右中央に縦ガイドを追加
- **既存ガイドを削除**：`grid_guides` レイヤー内の既存ガイドを削除

### 注意点

- ガイドは `grid_guides` レイヤー、セルの長方形は `cell-rectangle` レイヤーに作成され、［OK］後にガイドのレイヤーはロックされます。
- 「既存ガイドを削除」は `grid_guides` レイヤー内のガイドだけが対象です。対象が「すべてのアートボード」のときはすべて、それ以外はアクティブなアートボード上のガイドのみを削除します。
- マージンやガターが大きすぎてセルの幅・高さが0以下になる対象はスキップされます。
- 文字を編集中（文字を選択している状態）は、選択オブジェクトとして扱いません。

### 標準ツールでなく、このスクリプトを使うメリット

- アートボードを対象にする場合、アートボードサイズの長方形を描く手間が省ける
- すべてのアートボードを対象にできる
- アートボードに対して上下左右、個別にマージンを設定できる
- アートボードからどれだけ伸ばすか、指定できる

### 段組設定との違い

スクリプトの方は次のような特徴があります。

- アートボードが対象（段組設定はパスが対象）
- ガイドの長さを調整できる（段組設定はガイドが長い）
- 上下左右のマージンを個別に設定できる
- プリセットを登録できる
- 生成される長方形が別レイヤーに、不透明度を15%に設定

### note

https://note.com/dtp_tranist/n/n7adc7290b607

### 参考

すがさわくんβ  
https://note.com/sgswkn/n/nee8c3ec1a14c

### 更新履歴

- v1.0.0 (20250424) : 初期バージョン
- v1.0.1 (20250427) : ガイド、裁ち落としガイド、プリセット書き出しを追加
- v1.7.1 (20260827) : 「アートボードのエッジ」を追加、「既存ガイドを削除」をONにした時点で反映＋削除範囲を対象に合わせる、ガイドの重複描画を修正、空欄時のエラーとプレビューのモーダル表示を修正、プリセットの単位換算を実寸に修正、UIレイアウトと命名を整理
