# 登録済みAIファイルからスタイル・ブラシ・フォント見本を読み込み

[![Direct](https://img.shields.io/badge/Direct%20Link-ImportStyles.jsx-ffcc00.svg)](https://github.com/swwwitch/illustrator-scripts/blob/master/jsx/style/ImportStyles.jsx)

[![Direct](https://img.shields.io/badge/Back%20to%20home-All%20scripts-cccccc.svg)](https://github.com/swwwitch/illustrator-scripts/blob/master/README.md)

---

### 概要

- あらかじめ登録しておいた AI ファイルを一覧から選び、その中身を現在のドキュメントへ取り込むスクリプト。
- グラフィックスタイル、ブラシ、シンボル、フォント見本などを、オブジェクトごと貼り付けて持ち込める。
- 候補リストは TSV ファイル（`ImportStyles_candidates.tsv`）に永続化され、ダイアログから追加できる。

### フォルダーの設定

- スタイル用 AI ファイルを置くフォルダーは、初回起動時にフォルダー選択ダイアログで指定する（初期状態は未設定）。
- 指定したフォルダーは Illustrator の環境設定に保存され、次回以降は自動的に使われる。
- ダイアログの **フォルダー…** ボタンで、いつでも設定し直せる（切り替えると、そのフォルダーの候補 TSV を読み直す）。

### 主な機能

- 「コンテンツ」「ファイル名」の2列＋ヘッダ付き ListBox（スクロール可）で候補を表示
- **カテゴリー**ラジオ（**すべて** / **スタイル／ブラシ／シンボル** / **フォント**）で候補を絞り込み（初期値は**すべて**）
- 検索欄＋**検索**ボタンで、コンテンツ名とファイル名を対象に大文字小文字を無視して絞り込み（ボタン押下時のみ適用）
- ダブルクリックまたは Enter キー、**読み込み**ボタンで選択を確定
- ⌘F（Windows は Ctrl+F）で検索欄をアクティブに。検索欄で Enter を押すと検索を実行
- **追加**ボタン：任意の AI ファイルを選んでスタイル用フォルダへコピーし、表示名とカテゴリー（ファイル名から自動推定）を指定して候補 TSV に登録／上書き
- **ペースト先**ラジオ（**現在のレイヤー** / **指定レイヤー（// _imported）**）を選択（初期値は**現在のレイヤー**）。指定レイヤーは無ければ自動作成し、ロック解除・表示状態にする
- 候補 TSV は `label / path / category` の3列。旧形式の check 列（0|1）は読み込み時に無視
- TSV が存在しない場合は、設定したフォルダー内の `*.ai` を自動で一覧化（カテゴリーはファイル名から推定）
- 日本語／英語の自動切り替え

### 処理の流れ

1. ダイアログで候補から AI ファイルと貼り付け先を選択
2. 対象 AI を開き、作業アートボード内のオブジェクトをすべてコピー
3. 対象 AI を保存せずに閉じる
4. 元ドキュメントへ復帰し、貼り付け先レイヤーを用意して貼り付け

### 対象外

- 元のドキュメントが開かれていない場合（起動直後にアラートを表示して終了）
- 候補のパスにファイルが存在しない場合（アラートを表示して終了）
- 対象 AI の作業アートボード内にオブジェクトがない場合（アラートを表示して終了。クリップボードの残留物を貼り付けないため）
- 作業アートボードの外にあるオブジェクトはコピーされない
- Illustrator の仕様により、グラフィックスタイル／ブラシは最低1件が残る

### 更新履歴

- v1.0 (20250814) : 初期バージョン
- v1.1 (20250815) : CANDIDATES に削除オプションを記録
- v1.2 (20250815) : CANDIDATES を外部 TSV から読み込み可能に
- v1.3 (20250815) : カテゴリ列（スタイル／ブラシ／シンボル／フォント）対応、追加時にカテゴリ選択（ドロップダウン）
- v1.4 (20250815) : 標準 ListBox（2列ヘッダ）へ刷新、削除オプションを廃止、カテゴリラジオ＆検索ボタンを追加、貼り付け先を「// _imported」に統一、ドキュメント刷新
- v1.5.0 (20260701) : ローカライズを構造化（ネスト LABELS ＋ ドット区切り L()）、全体を IIFE 化、パネル共通設定（setupPanel）を追加、変数名・関数名を整理、追加フローを関数分割、重複コード・不要な try を削減
- v1.5.1 (20260802) : スタイル用フォルダーをスクリプト内固定から環境設定への保存へ変更（初回起動時に選択、［フォルダー…］で変更可）、ペースト先ラジオ（現在のレイヤー／指定レイヤー、既定は現在のレイヤー）を追加、カテゴリーに「すべて」を追加、⌘F で検索欄へ移動、作業アートボードが空のときの誤ペーストを防止、ドキュメント未オープンの判定を起動直後へ移動、TSV がない場合はフォルダー内の AI ファイルを自動列挙、ロック／非表示レイヤーへのペーストを事前に中止
