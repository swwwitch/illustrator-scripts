# KPTSketchy

[![Direct](https://img.shields.io/badge/Direct%20Link-KPTSketchy.jsx-ffcc00.svg)](https://github.com/swwwitch/illustrator-scripts/blob/master/jsx/fx/KPTSketchy.jsx)

[![English](https://img.shields.io/badge/README-English-4b8bbe.svg)](https://github.com/swwwitch/illustrator-scripts/blob/master/readme-en/KPTSketchy.md)

[![Direct](https://img.shields.io/badge/Back%20to%20home-All%20scripts-cccccc.svg)](https://github.com/swwwitch/illustrator-scripts/blob/master/README.md)

---

### 概要

- 選択したオブジェクトにランダムな変形を加え、手書き・スケッチ風の見た目に整えます。
- 塗りと線の分離、角丸、パスのオフセット、ラフ効果（細かい揺れ／大きなうねり）、グループ化に対応します。
- 変形はすべてライブ効果として適用するため、あとから編集・解除できます。
- 常駐パレットとして表示され、開いたままドキュメントを操作できます。

### 使い方

1. スクリプトを実行するとパレットが表示されます。
2. オブジェクトを選択し、各パネルで効果を調整します（変更するたびに結果が作り直されます）。
3. ［振り直す］で乱数を振り直します。気に入ったらそのままにしておくだけで確定です。
4. パレットから離れる、または閉じる（× / Esc）と、その時点の結果が確定します。

### パネル

| パネル | 内容 |
| --- | --- |
| 適用対象 | グループ内を個別に処理するか、塗りと線を分離するか、分離後にグループ化するか |
| ランダム変形 | 倍率・移動・回転の振れ幅 |
| 角丸・オフセット | 角丸の半径、パスのオフセット量 |
| ラフ：細かい揺れ | 輪郭を細かく震わせ、ペンで描いたような線にする |
| ラフ：大きなうねり | 輪郭を大きくうねらせ、形そのものを崩す |

### 仕様

- 角丸・オフセット・移動は、環境設定の単位（rulerType）で入力します。
- ［ランダム変形］の3項目は ±値 の振れ幅です。単位表示に付く `±` がその印です。
- ［ラフ：細かい揺れ］［ラフ：大きなうねり］はどちらも［ラフ］効果です。
  Illustratorの［ラフ］でいう「ポイント：丸く」に固定しているため、2つの違いはサイズと詳細の大小だけです。
- 適用順は「角丸 → パスのオフセット → 変形 → ラフ（大きなうねり → 細かい揺れ）」です。
- オフセットは負方向 → 正方向の順に 2 回適用します。
- 塗りと線の両方を持つオブジェクトのみ分離できます（塗りのみ／線のみは分離しません）。
- グループは「グループ内を個別に処理」ON のとき、中の各オブジェクトへ展開します。
- クリッピングパスは処理対象から除外します。
- 数値欄は負の値を 0 に、詳細欄は 1 以上の整数に丸め、入力欄の表示も実際に適用される値へ揃えます。
- 数値として読めない入力（`3abc` など）は適用せず、状況表示で知らせます。
- 値の変更・再計算では、直前の結果を app.undo() で取り消してから適用し直します。
- パレットからフォーカスが外れた時点で結果を確定し、以降は取り消しません
  （ユーザーがドキュメント側で行った操作を誤って取り消さないため）。
- 応答がない（タイムアウト）場合は、適用が終わっているかどうか判断できないため自動取り消しを止めます。
  効果が二重に適用されていれば手動で取り消してください。

### 実装メモ

- 常駐パレットの app はドキュメントへの接続を失うため、DOM を触る処理は
  すべて worker 関数にまとめ、BridgeTalk でメインエンジンへ委譲します。
- worker 関数は toString() で連結し、encodeURIComponent したうえで
  eval(decodeURIComponent(...)) の形で送信します。
- worker 関数内では行コメントを使わず、必ずセミコロンで文を終えます。
- パレットの参照は `$.global` に載せて常駐エンジンに残します。
  関数スコープの `var` は巻き上げで必ず `undefined` から始まるため、
  `var x = (typeof x !== "undefined") ? x : null` の形では前回の値を持ち越せません。

### 紹介記事

[【Illustrator】手書きタッチや少し崩した感じにするスクリプト｜DTP Transit 別館](https://note.com/dtp_tranist/n/na808bac430d9)

### 更新履歴

- v1.2.1 (2026-09-05): パレット参照を `$.global` に保持して再実行時に前回のパレットを確実に閉じるよう修正、
  タイムアウト・送信失敗時の自動取り消しを見直し、数値入力の検証と表示の正規化、
  UI文言の見直し（［ラフ］パネル名、［振り直す］ほか）、命名とコード構成の整理
- v1.2.0 (2026-07-22): 常駐パレット化、DOM操作を BridgeTalk でメインエンジンへ委譲、2カラムUIへ再構成
- v1.1.0 (2026-04-14): 変形（倍率・移動・回転）とパスのオフセットを追加
- v1.0.0 (2026-04-14): 初回リリース

---

### スクリプト情報

- バージョン: v1.2.1
- 初回リリース: 2026-04-14
- 最終更新: 2026-09-05
