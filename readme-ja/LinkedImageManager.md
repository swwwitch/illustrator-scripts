# リンク画像の管理

[![Direct](https://img.shields.io/badge/Direct%20Link-LinkedImageManager.jsx-ffcc00.svg)](https://github.com/swwwitch/illustrator-scripts/blob/master/jsx/link/LinkedImageManager.jsx)

[![Direct](https://img.shields.io/badge/Back%20to%20home-All%20scripts-cccccc.svg)](https://github.com/swwwitch/illustrator-scripts/blob/master/README.md)

---

入稿前のチェックで、リンク画像まわりを一通り確認する場面があります。リンクは切れていないか、解像度は足りているか、RGB のままの画像が混ざっていないか、同じ画像を何箇所で使っているのか。

Illustrator の［リンク］パネルでも1つずつ選べば分かるのですが、**一覧で見比べられない**のが厄介なところです。PPI を確認したければ画像を1つ選んでパネルを展開し、次の画像を選んでまた展開し、の繰り返しになります。「PPI が低い順に並べたい」といった見方はできません。

そこで、配置画像を全部まとめて解析して**表として並べ、ソートと絞り込みができる**常駐パレットにしました。一覧から再リンクやリネーム、削除まで済ませられます。**埋め込み画像も同じ一覧に並び**、リンクへの切り替え（埋め込み解除）とその逆（埋め込み）も同じ場所で操作できます。

<img alt="" src="" width="50%" />

## 使い方

スクリプトを実行するとパレットが開き、ドキュメント内の配置画像（PlacedItem）と埋め込み画像（RasterItem）が一覧表示されます。

実行前に配置画像を1つ選択していた場合は、対応する行が最初から選択された状態で開きます。このときカンバスのビューは動きません。

ドキュメントを開いていない状態でも、パレット自体は開きます。あとからドキュメントを開いて［更新］ボタンを押せば読み込まれます。配置画像が0件のときやドキュメント未オープンのときは、オプション類とリストを隠した簡易表示になります。読み込みに失敗したときは、その理由がパレット下部のステータス行に出ます。

パレットは `Esc` キーでも閉じられます。

## 一覧の見かた

先頭のアイコン列とファイル名列は常に表示され、それ以外の列は［表示列］パネルのチェックボックスで出し入れします。

| 列 | 内容 |
| --- | --- |
| アイコン | ✓ リンク正常 / ⚠ リンク切れ / ⟳ 更新が必要 / ▣ 埋め込み |
| ファイル名 | リンクファイル名 |
| サイズ | ファイルサイズ |
| 使用数 | 同じファイルが何箇所に配置されているか |
| 幅(mm) / 高さ(mm) | 配置されている実寸 |
| スケール | 配置時の拡大縮小率 |
| PPI | 実効解像度 |
| カラースペース | カラーモード（＋ICC プロファイル名） |
| アートボード | 何番のアートボードに乗っているか |

### 埋め込み画像の行

埋め込み画像は「▣ 埋め込み」として並びます。リンク元のファイルを持たないため、**ファイルパス・サイズ・PPI は `---`** になります。ファイル名はアイテム名（埋め込み時に元のファイル名が残ることが多い）から拾い、カラースペースは `imageColorSpace` から取ります。

ラスタライズしたオブジェクトも埋め込み画像として並びます。埋め込んだ PSD をグループ解除している場合は、レイヤーごとに1行ずつ並びます。

### ステータスの判定

「⟳ 更新が必要」は、ドキュメントの XMP メタデータに記録されているリンク時の更新日時と、実ファイルの現在の更新日時を比べて判定しています。**5秒を超える差**があれば「更新が必要」です。ファイルが存在しなければ「⚠ リンク切れ」になります。

### PPI とスケール

PPI は、リンクファイルの実ピクセル数と配置寸法から `ピクセル数 × 72 ÷ 配置寸法(pt)` で算出し、縦横の平均を四捨五入した値です。［リンク］パネルの表示と同じ考え方ですが、一覧で並べてソートできます。

ピクセル数の取得には、リンクファイルのバイナリを直接読んでいます。対応しているのは **PNG / JPEG / PSD** です。それ以外の形式（AI、PDF、TIFF など）では PPI とカラースペースが `---` になります。

スケールは配置アイテムの変形マトリックスから求めています。縦横の倍率が 0.1% 以上違うときだけ `80.0% × 60.0%` のように両方を表示し、揃っていれば1つだけ表示します。

### カラースペース

リンクファイルから読み取ったカラーモードと、埋め込まれた ICC プロファイル名を `RGB（sRGB IEC61966-2.1）` のような形で表示します。プロファイルが無ければカラーモードだけです。こちらも PNG / JPEG / PSD が対象です。

## 同一ファイルをまとめる

初期状態では **同じリンクファイルを1行にまとめて** 表示します。ファイルパスが一致するものが同一ファイルの扱いです。「使用数」列にその件数が出て、「アートボード」列には配置されているアートボード番号がまとめて（`1, 3, 5` のように）並びます。

チェックを外すと、配置ごとに1行ずつの表示に切り替わります。

なお**リンク切れの画像はまとめられません**。パスが取れないため、同じ名前に見えても別々の行として出ます。

初期のソート順は「使用数」の降順です。使い回している画像から目に入るようにしています。

## ソート

［ソート］パネルの並び順ドロップダウンと、昇順／降順のラジオボタンで並び替えます。

| 並び順 | 備考 |
| --- | --- |
| ファイル名 | |
| サイズ | 「サイズ」列の表示中のみ |
| 使用数 | 「使用数」列の表示中のみ |
| アートボード | アートボードで絞り込んでいないときのみ |
| 幅 / 高さ / スケール / PPI | 「サイズ、%、PPI」列の表示中のみ |
| ステータス | リンク切れ → 更新が必要 → 正常 → 埋め込み の順 |
| カラースペース | 「カラースペース」列の表示中のみ |

**ドロップダウンの項目は表示列に連動して増減します。** 表示していない列では並べ替えられないためです。列を出し入れしても、選んでいた並び順は可能な限り維持されます。

サイズ・使用数・幅・高さ・スケール・PPI を選ぶと、**自動的に降順に切り替わります**。これらは「大きいもの」「多いもの」から見たいことがほとんどだからです。ファイル名やステータスを選んだときは切り替わりません。

値が取れなかった行（PPI が `---` など）は、昇順・降順どちらでも常に末尾にまとまります。

## 絞り込み

### ステータス

「✓ リンク正常」「⚠ リンク切れ」「⟳ 更新が必要」「▣ 埋め込み」の4つのチェックボックスで、表示するステータスを選びます。リンク切れだけを見たいときは他の3つを外します。すべて初期状態でオンです。

### アートボード

ドロップダウンでアートボードを選ぶと、そのアートボード上の画像だけに絞り込まれます。

**画像が1枚も乗っていないアートボードはディム（グレーアウト）されて選べません。** どのアートボードを見れば良いかが、開いた時点で分かるようにしています。

アートボードを選ぶと、**カンバスのビューもそのアートボードにフィット**します。一覧の絞り込みと画面の表示が連動する形です。

隣の ◀ ▶ ボタンでアートボードを送れます。ディムされているアートボードは自動的に読み飛ばし、末尾まで行くと先頭に戻ります（「すべて」には戻りません）。選べるアートボードが1つ以下のときはボタンごと無効になります。

なお**アートボードで絞り込んでいる間は「アートボード」列が消えます**。全部同じ番号になり、意味が無いためです。

画像がどのアートボードに属するかは、画像の可視バウンズの中心座標がどのアートボードの矩形に入るかで判定しています。どこにも入らなければ `-` です。

## 選択との連動

行を選ぶと、対応する配置画像がカンバス上でも選択され、画面いっぱいにズーム・センタリングされます。同一ファイルをまとめている行なら、その全ての配置がまとめて選択されます。

ズームが不要なら［選択時にズーム表示］のチェックを外してください。

逆に、**カンバスで選んだ画像に一覧の行を合わせる**こともできます。Illustrator は選択の変更をスクリプトへ通知しないため、常時追従はできません。カンバスで画像を選んでからパレットをクリックした時点（パレットがアクティブになったとき）に、その選択を取りに行って対応する行を選び直します。単独選択のときだけが対象で、このときカンバスのビューは動きません。

## ファイルパスの表示

リストで選んだ行のパスが［ファイルパス］パネルに出ます。3つのチェックボックスで表示を切り替えます。

| チェック | 動作 |
| --- | --- |
| フルパス | 絶対パスをそのまま表示 |
| Dropboxパスを短縮 | Dropbox のマウントパスの接頭辞を省く |
| ファイル名 | ON でファイル名まで、OFF ならフォルダーまで |

初期状態では「フルパス」「ファイル名」がオフ、つまり**ホームディレクトリを `~` に置き換えたフォルダーパス**が表示されます。パスが長くなりがちなので、既定では短い表示にしています。

「Dropboxパスを短縮」を ON にしている間は「フルパス」がディムになります（短縮とフルパスは両立しないため）。

Dropbox の短縮は、スクリプト冒頭の `DROPBOX_PREFIX` に自分のローカルマウントパスを設定して使います。`""` にしておくと**チェックボックス自体が表示されません**。

## リンクフォルダー一覧

配置画像が参照しているフォルダーが、重複を除いて一覧されます。リストで行を選ぶと、そのファイルが入っているフォルダーが自動的にハイライトされます。

ダブルクリック、または［開く］ボタンで Finder で開きます。

## 画像ごとの操作

リストで行を選んでいるときに使えるボタンです。

- **［ファイル名をコピー］**：ファイル名をクリップボードにコピー
- **［開く］**：リンクファイルを関連付けられたアプリで開く
- **［リネーム］**：物理ファイル名の変更＋再リンク
- **［削除］**：配置画像をドキュメントから削除
- **［再リンク］／［一括再リンク］**：リンク先の差し替え
- **［埋め込み］**：リンク画像を埋め込み画像に変換
- **［埋め込み解除］**：埋め込み画像をリンク画像に戻す

埋め込み画像を選んでいるときは、リンク元のファイルを前提とする操作（開く・リネーム・再リンク・埋め込み）が無効になり、［埋め込み解除］だけが有効になります。［削除］と［ファイル名をコピー］はどちらの行でも使えます。

### リネーム

**物理ファイルのリネームと再リンクを同時に行います。** ファイルの名前を変えるとリンクが切れる、という手間を1操作にまとめたものです。

拡張子は自動で保持されます。入力欄には拡張子を除いた部分だけが入っていて、うっかり拡張子まで入力しても取り除かれます。同名のファイルが既にある場合は上書き確認が出ます。

同一ファイルをまとめている行なら、その全ての配置がまとめて新しい名前へ再リンクされます。

### 削除

削除対象が**クリップグループ（マスク）の中にある**とき、専用のダイアログが出て2つの削除方法から選びます。

| 選択肢 | 動作 |
| --- | --- |
| 画像のみを削除 | 配置画像だけを消し、クリップグループは残す |
| クリップグループごと削除 | 画像を囲むクリップグループごと消す |

クリップグループの外にある画像なら、通常の確認アラートだけです。

### 再リンク

ボタンのラベルは状況で変わります。同一ファイルをまとめていて、かつその行の配置が複数あるときは **［一括再リンク］** になり、押すと確認のうえ全ての配置を選んだファイルへ差し替えます。配置が1つなら［再リンク］です。

### 埋め込み

選んだリンク画像を埋め込み画像に変換します。同一ファイルをまとめている行なら、確認のうえその全ての配置をまとめて埋め込みます。

**PSD は埋め込むとレイヤーがグループになる**ため、クリップグループの中にない場合に限りグループ解除まで行います。クリップグループ内の画像は、マスクの構造を壊さないようグループのままにします。

解除は DOM 操作で行っています。Illustrator は埋め込んだラスターをロックした状態でクリップグループに入れるため、メニューコマンドの「グループ解除」は選択できずに失敗するためです。解除時はマスクがアートを覆いきっている場合だけマスクパスを削除し、ラスターのロックも外します。マスクが実際にトリミングしている場合は、見た目が変わるためグループのまま残します。

埋め込んだ画像には、一覧表示と埋め込み解除の名前復元のために拡張子つきの元ファイル名を付けます。ただし**親グループが既に同じ名前を持つときは付け直しません**。レイヤーパネルに同じ名前が親子で並んでしまうためです。

### 埋め込み解除

選んだ埋め込み画像をリンク画像に戻します。一時アクション（`adobe_placeDocument`）を動的に生成し、「選択オブジェクトと置換」で配置するため、**位置・サイズ・回転角・重ね順は Illustrator 側がそのまま引き継ぎます**。

リンク先は次の順で決まります。

1. 埋め込み画像が元ファイルの情報を持っていて、そのファイルが存在すればそこへリンク
2. 元ファイルが不明なら、その画像を**等倍・回転0°に戻して `Links` フォルダーへ PSD として書き出し**、そこへリンク

書き出し名は、レイヤー名 → 拡張子付きの親グループ名 → XMP マニフェストの元ファイル名 → `image1`、`image2`… の順に探します。同名ファイルがあれば連番を付けるので、既存ファイルは上書きしません。

**［再リンク後に収集］**（初期状態でオン）は［埋め込み解除］のオプションです。［埋め込み解除］が使えない行を選んでいる間はディムになります。オンにしておくと、リンク先をドキュメントと同階層の `Links` フォルダーへコピーして張り替えます。同名で内容が異なるファイルには連番を付け、同一とみなせるファイルは再コピーしません。PSD を書き出した画像はすでに `Links` フォルダーにあるため、二重にコピーはしません。

ロックまたは非表示のレイヤー・グループ・画像は選択できないため置換できません。未対応のカラースペース（CMYK / RGB / グレースケール以外）はスキップされ、理由がアラートにまとめて表示されます。

PSD 書き出しを伴うため、ドキュメントが未保存だと `Links` フォルダーの場所を決められず失敗します。

## フォルダー単位の操作

リンクフォルダー一覧で行を選んでから使います。

### フォルダー再リンク

選んだフォルダーを参照している画像を、**別フォルダーの同名ファイルへまとめて差し替えます**。移動先フォルダーに同名ファイルが無いものはスキップされ、件数がステータス行に出ます。

### 拡張子の変更

参照フォルダーと拡張子を選ぶと、**ファイル名の本体部分が同じで拡張子だけ違うファイル**を探して再リンクします。PSD で作業していたものを入稿用に別形式へ差し替える、といった用途です。

選べる拡張子は png / jpg（jpeg）/ psd / tiff（tif）/ webp / avif / gif / ai / pdf です。既定は psd。

`jpg / jpeg` と `tiff` は**2つの拡張子を順に探します**。まず `.jpg`（`.tif`）を探し、無ければ `.jpeg`（`.tiff`）を探す、という順序です。拡張子の大小文字は無視されます。

すでに同じファイルを参照している画像はスキップ扱いになり、対象／成功／スキップ／失敗の件数がステータス行に出ます。

### リンクを収集

**ドキュメントと同じ階層に `Links` フォルダーを作り、リンクファイルをコピーして再リンクします。** 入稿データをまとめるときの操作です。

`Links` フォルダー内に同名ファイルが既にあればコピーはせず、その既存ファイルへ再リンクします。すでに `Links` フォルダー内を参照している画像はスキップです。

ドキュメントが未保存だと、置き場所が決まらないため実行できません。

## その他のボタン

- **［［リンク］パネルを開く］**：Illustrator 標準の［リンク］パネルを開きます
- **［更新］**：ドキュメントから読み込み直します

パレットは開いたままなので、カンバス側で画像を差し替えたり配置し直したりしたら［更新］を押してください。再読み込み後も、選んでいた行は可能な限り選択し直されます。

## 対象

ドキュメント内の PlacedItem（配置されたリンク画像）と RasterItem（埋め込み画像）。

PPI とカラースペースの取得は PNG / JPEG / PSD のみ対応しています。埋め込み画像は元ファイルを読めないため PPI が出ません。

## 補足

### 常駐パレットと BridgeTalk

`#targetengine` による常駐エンジンで動作します。常駐エンジンの `app` は、パレットを表示している間に DOM への接続を失うため、選択取得・配置解析・移動・属性変更といった DOM を触る処理はすべて worker 関数にまとめ、操作のたびに BridgeTalk でメインエンジン（`target=illustrator`）へ委譲しています。

worker 関数は `WORKER_FUNCS` に全登録し、初回に一度だけメインエンジン側へ常駐させます（`$.global.__LIM`）。以降は呼び出し式だけを送ります。毎回すべての関数本体を送ると、行数が多いぶん目に見えて遅くなるためです。**worker 関数を追加したら `WORKER_FUNCS` への登録も必要**です。登録漏れは委譲側だけが無言で壊れます。

worker 関数の本体は `toString()` で連結して送りますが、**ExtendScript の `toString()` は書いたとおりのソースを返しません**。実測では次の2点が起こります。

- 行区切りが CR（`\r`）で返る。`eval` は LF でしか行コメントを終端しないため、関数内の `//` コメントが連結境界の LF まで後続コードを飲み込む。
- 関数本体の前後に周辺コメントの断片が、**閉じ `*/` を欠いた状態で**混ざる。未終端コメントが次の関数の先頭を飲み込む。

どちらもメインエンジン側の eval が構文エラーになり、`$.global.__LIM` が常駐しないまま**すべての委譲が無言で失敗**します。そのため連結時に `sliceFunctionSource()` を通し、改行を LF に正規化したうえで `function` の宣言行から閉じ括弧だけの行までを行単位で抜き出しています。

あわせて、worker 関数を書くときの制約は次のとおりです。

- 行コメント（`//`）は使わない。ブロックコメントのみ。
- JSDoc（`/** ... */`）は付けない。
- 文は必ずセミコロンで終える。
- 相互参照は `w_` プレフィックス名で行う（同一 IIFE クロージャ内に置かれる）。

戻り値はマーカー方式です。`OK\n<json>` / `NODOC` / `ERR\n<msg>` を先頭行で判別します。

### itemIndex の持ち方

一覧の行は、リンク画像なら `placedItems` の添字、埋め込み画像なら `rasterItems` の添字を `itemIndex` として持ちます。2つのコレクションで添字が衝突するため、**埋め込み画像側は 1000000 を足した値**にしてあります。worker 側は `w_itemByIndex()` でこの値を見て、どちらのコレクションから引くかを決めます。パレット側は整数のまま扱えるので、選択の復元や照合のコードはそのまま使えます。

### 埋め込み解除のタイムアウト

BridgeTalk の送信は既定で10秒待ちですが、埋め込み解除は PSD 書き出しとアクション実行を含むため間に合わないことがあります。この呼び出しだけ300秒まで待ち、**タイムアウトしても再送しません**。書き込みを伴う処理を再送すると二重に配置されてしまうためです。

### パレット側で完結する処理

ファイルの存在確認・リネーム・コピーといった `File` 操作は常駐エンジンでも動くため、パレット側で完結させています。再リンクは「リンク先の差し替え」だけを委譲する形です。

一方、埋め込み・埋め込み解除は DOM 操作の比重が大きいため、一連の処理をまとめて worker 側で実行しています。埋め込み解除の PSD 書き出し（一時ドキュメントの作成と `exportFile`）と一時アクションの生成・実行も worker 側です。

### その他

ファイル名のクリップボードコピーは、一時的なテキストフレームを作って `app.copy()` する方式です。コピー後は元の選択状態に戻します。

アートボード送りの ◀ ▶ は、フォントのグリフに依存しないよう、ボタン面に三角形をベクターで自前描画しています。UI の明暗設定も見て色を変えています。

### note

- [【Illustrator】スクリプトで行う［リンク］パネルの再設計｜DTP Transit 別館](https://note.com/dtp_tranist/n/na66732d2056a)

## 更新履歴

- v1.5.3（2026-08-02）：カンバスで選んだ画像に一覧の行を合わせる逆同期を追加（パレットをアクティブにした時点で同期）。埋め込み後の PSD がグループ解除されずラスターがロックされたままになる問題を修正。埋め込み時に親グループと同じ名前をラスターにも付けていた問題を修正。［埋め込み］［埋め込み解除］［再リンク後に収集］をファイル操作ボタンの直下へ移動し、［再リンク後に収集］を［埋め込み解除］とディム連動
- v1.5.2（2026-08-02）：worker 関数の連結ソースが壊れて委譲がすべて失敗する問題を修正。簡易表示でパレットが縮まず余白だけが残る問題、読み込み失敗の理由が表示されない問題も修正。パネルの余白・間隔を共通設定に統一
- v1.5.1（2026-07-27）：埋め込み時にファイル名の拡張子が失われる問題を修正
- v1.5.0（2026-07-27）：埋め込み画像を一覧に追加（ステータス「▣ 埋め込み」と絞り込み）、［埋め込み］［埋め込み解除］［再リンク後に収集］を追加
- v1.4.2（2026-07-27）：ファイル名のURIデコード漏れを修正、［更新］時にアートボード一覧を再構築、読み込みの再入・多重実行の防止
- v1.0.0（2026-04-24）：初版
