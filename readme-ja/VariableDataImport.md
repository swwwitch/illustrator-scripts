# VariableDataImport.jsx

[![Direct Link](https://img.shields.io/badge/Direct%20Link-VariableDataImport.jsx-ffcc00.svg)](https://github.com/swwwitch/illustrator-scripts/blob/master/jsx/data/VariableDataImport.jsx)

[![English](https://img.shields.io/badge/README-English-4b8bbe.svg)](https://github.com/swwwitch/illustrator-scripts/blob/master/readme-en/VariableDataImport.md)

[![Back to home](https://img.shields.io/badge/Back%20to%20home-All%20scripts-cccccc.svg)](https://github.com/swwwitch/illustrator-scripts/blob/master/README.md)

---

## 概要

CSV / タブ区切りテキストのデータを、Illustratorのテンプレートに流し込むデータ結合スクリプトです。

テキストフレーム内に置いた `<ヘッダー名>` 形式のタグをデータ値に置き換え、データ件数ぶんのアートボードとバリエーションを一度に生成します。名刺、席札、会員証、値札、賞状など「同じデザインで中身だけ違う」制作物に使えます。

元ドキュメントは別名保存で複製し、複製側に流し込むため、元ファイルは変更されません。

## 主な機能

- 開いているドキュメントと同じフォルダーにある CSV / TSV ファイルから選択
- 読み込んだデータをダイアログ内の一覧で確認
- `<ヘッダー名>` タグをデータ値へ置換（タグに設定した文字書式は保持）
- データ件数ぶんのアートボードを自動生成
- グリッド全体が正方形に近くなる列数を自動計算し、カンバスの天地・左右中央に配置
- アートボード間隔の指定（10pt単位に丸め）
- アートボード名に使うデータ列を選択
- プレビュー（元ファイルを変更せず、複製ファイルで結果を確認）
- UTF-8（BOM有無どちらも）と Shift-JIS のデータファイルに対応
- 日本語／英語UI

## 使い方

1. 雛形にするデザインを **アートボード1** に作成します。差し替えたい箇所は、テキストフレーム内に `<氏名>` `<会社名>` のようにタグで書いておきます。
2. データファイル（CSV / タブ区切りテキスト）を、ドキュメントと **同じフォルダー** に置きます。1行目がヘッダー行で、その列名がタグ名に対応します。
3. ドキュメントを保存します（未保存だと実行できません）。
4. `VariableDataImport.jsx` を実行します。
5. ダイアログで設定します。
   - ［ファイル］: 読み込むデータファイル
   - ［アートボード名に使う列］: 各アートボードの名前に使う列
   - ［アートボード間隔］: 複製するアートボード同士の間隔（縦横とも同じ間隔、単位はpt）
6. ［プレビュー］をオンにすると、複製ファイルで結果を確認できます。オフにすると閉じます。
7. ［複製して実行］をクリックします。元ドキュメントが別名保存で複製され、複製側にデータが流し込まれます。

## データファイルの書式

| 拡張子 | 区切り文字 |
| --- | --- |
| `.csv` | カンマ（引用符 `"` と `""` エスケープに対応） |
| `.txt` | タブ |

```
氏名,会社名,役職
山田太郎,株式会社スイッチ,代表
鈴木花子,株式会社サンプル,デザイナー
```

このデータに対して、雛形のテキストフレームに `<氏名>` `<会社名>` `<役職>` と書いておくと、それぞれの値に置き換わります。

- 1行目のヘッダー名が、そのままタグ名になります。
- 各値の前後の空白は自動で除去されます。
- 値が空の行は、アートボード名が `Data_1` `Data_2` … になります。

## 生成されるファイル

| 操作 | 生成されるファイル |
| --- | --- |
| ［複製して実行］ | `元のファイル名_import_YYYYMMDD_HHMMSS.ai` |
| ［プレビュー］ | `元のファイル名_preview_YYYYMMDD_HHMMSS.ai`（プレビューをオフにすると削除） |

いずれも元ドキュメントと同じフォルダーに作られます。元ファイルは変更されません。

## 注意事項

- 雛形として複製されるのは、アートボード1上にある **ロックも非表示もされていない** オブジェクトだけです。ロックレイヤーの背景などはバリエーションに複製されません。
- ［プレビュー］はディスク上のファイルを複製するため、保存していない編集は反映されません。
- 引用符で囲まれたフィールド内の改行には対応していません。1レコード1行で書き出してください。
- アートボードの生成数は最大1000件です。データ件数がカンバスに収まらない場合は、アートボード間隔を小さくしてください。
- 1件目のデータは最後に処理します（雛形のタグが先に消えるのを避けるため）。
- タグはテキストフレームとグループ内のテキストフレームを対象に置換します。シンボルや埋め込み画像内のテキストは対象外です。

## 紹介記事

[【Illustrator】CSVのデータを流し込むスクリプト｜DTP Transit 別館](https://note.com/dtp_tranist/n/n741c9f28d0fd)

## 更新履歴

- v1.4.1 (2026-05-18): 安定性の改善
- v1.4.0 (2026-05-15): プレビュー機能を追加
- v1.3 (2026-01-22): アートボードのグリッド配置を自動計算に変更
